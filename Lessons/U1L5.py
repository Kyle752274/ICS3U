import math
m = math 
length = input("Please input a length: ")
length = float(length)
r= input("Please input a radius")
r= float(r)
area = 0.5*math.pi*math.pow(r,2) + 4*math.pow(r,2)
print("The area of a square of side length", length, "is:", area)
